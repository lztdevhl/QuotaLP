# Optional: override with the real production origin (https://your-domain).
# Leave empty to use VERCEL_PROJECT_PRODUCTION_URL automatically on Vercel.
# Local builds use http://localhost:3000 and remain noindex.
NEXT_PUBLIC_SITE_URL=
